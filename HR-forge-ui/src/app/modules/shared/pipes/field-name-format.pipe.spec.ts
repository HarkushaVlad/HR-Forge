import { FieldNameFormatPipe } from './field-name-format.pipe';

describe('FieldNameFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldNameFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
